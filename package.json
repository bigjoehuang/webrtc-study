{
  "name": "webrtc-study",
  "version": "1.0.0",
  "description": "一个从基础到进阶的 WebRTC 学习项目",
  "main": "index.html",
  "scripts": {
    "start": "npx live-server --port=8000 --open=/index.html --watch=/",
    "dev": "npx concurrently \"npx live-server --port=8000 --open=/index.html --watch=/\" \"node scripts/start-signaling.js\"",
    "signaling": "node scripts/start-signaling.js"
  },
  "keywords": [
    "webrtc",
    "learning",
    "tutorial",
    "web-rtc"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "http-server": "^14.1.1",
    "live-server": "^1.2.2"
  },
  "dependencies": {
    "ws": "^8.16.0"
  }
}
